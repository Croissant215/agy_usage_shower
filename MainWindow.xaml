<Window x:Class="AgyUsageShower.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AGY Taskbar Widget"
        Width="330" Height="30"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Border Background="#181825" CornerRadius="6" BorderBrush="#313244" BorderThickness="1" Margin="1" Cursor="Hand">
        <Border.ToolTip>
            <ToolTip Content="AGY Quota Monitor: Gemini 5h 29.17% (7m), Weekly 35.09%, Claude 100%. Click for details."/>
        </Border.ToolTip>

        <Grid Margin="6,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Brand -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="2,0,6,0">
                <TextBlock Text="⚡" FontSize="11" Margin="0,0,3,0"/>
                <TextBlock Text="AGY" Foreground="#89B4FA" FontSize="11" FontWeight="Bold"/>
            </StackPanel>

            <Rectangle Grid.Column="1" Fill="#313244" Width="1" Margin="0,4"/>

            <!-- Gemini 5h & Weekly -->
            <Grid Grid.Column="2" VerticalAlignment="Center" Margin="6,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 5h Limit -->
                <DockPanel Grid.Row="0">
                    <TextBlock Text="5h" Foreground="#89B4FA" FontSize="9" FontWeight="Bold" Width="16"/>
                    <TextBlock Text="{Binding Quota5hFormatted}" Foreground="#CDD6F4" FontSize="9" FontWeight="SemiBold" Width="36"/>
                    <ProgressBar Height="3" Value="{Binding CurrentUsage.Gemini5hPercent}" Minimum="0" Maximum="100" Margin="2,0,4,0" Foreground="{Binding Color5hHex}" Background="#313244" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUsage.GeminiResetTime}" Foreground="#F9E2AF" FontSize="8.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Weekly Limit -->
                <DockPanel Grid.Row="1" Margin="0,1,0,0">
                    <TextBlock Text="Wk" Foreground="#CBA6F7" FontSize="9" FontWeight="Bold" Width="16"/>
                    <TextBlock Text="{Binding WeeklyFormatted}" Foreground="#CDD6F4" FontSize="9" FontWeight="SemiBold" Width="36"/>
                    <ProgressBar Height="3" Value="{Binding CurrentUsage.GeminiWeeklyPercent}" Minimum="0" Maximum="100" Margin="2,0,4,0" Foreground="{Binding ColorWeeklyHex}" Background="#313244" VerticalAlignment="Center"/>
                    <TextBlock Text="7m" Foreground="#A6ADC8" FontSize="8.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>
            </Grid>

            <Rectangle Grid.Column="3" Fill="#313244" Width="1" Margin="0,4"/>

            <!-- Claude & GPT Status -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,2,0">
                <TextBlock Text="Claude " Foreground="#A6ADC8" FontSize="9.5"/>
                <TextBlock Text="100%" Foreground="#A6E3A1" FontSize="9.5" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
