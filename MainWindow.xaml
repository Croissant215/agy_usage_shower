<Window x:Class="AgyUsageShower.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AGY Taskbar Widget"
        Width="400" Height="32"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Border Background="{Binding WidgetBgHex}" CornerRadius="6" BorderBrush="{Binding WidgetBorderHex}" BorderThickness="1" Margin="1" Cursor="Hand">
        <Border.ToolTip>
            <ToolTip Content="AGY Quota Monitor. Click for details."/>
        </Border.ToolTip>

        <Grid Margin="8,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Theme Toggle -->
                <ColumnDefinition Width="Auto"/> <!-- Brand -->
                <ColumnDefinition Width="Auto"/> <!-- Separator -->
                <ColumnDefinition Width="*"/>    <!-- Gemini 5h & Wk -->
                <ColumnDefinition Width="Auto"/> <!-- Separator -->
                <ColumnDefinition Width="Auto"/> <!-- Claude -->
            </Grid.ColumnDefinitions>

            <!-- Theme Toggle Button -->
            <Button Grid.Column="0" Content="{Binding ThemeIcon}" Click="ThemeToggle_Click" Background="Transparent" BorderThickness="0" FontSize="11" Margin="0,0,6,0" Cursor="Hand"/>

            <!-- Brand -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,6,0">
                <TextBlock Text="⚡" FontSize="11" Margin="0,0,3,0"/>
                <TextBlock Text="AGY" Foreground="{Binding BrandPinkHex}" FontSize="11" FontWeight="Bold"/>
            </StackPanel>

            <Rectangle Grid.Column="2" Fill="{Binding DividerHex}" Width="1" Margin="0,4"/>

            <!-- Gemini 5h -->
            <Grid Grid.Column="3" VerticalAlignment="Center" Margin="8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="44"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="5h" Foreground="{Binding BrandPinkHex}" FontSize="9.5" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Quota5hFormatted}" Foreground="{Binding MainTextHex}" FontSize="9.5" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <ProgressBar Grid.Column="2" Height="4" Value="{Binding CurrentUsage.Gemini5hPercent}" Minimum="0" Maximum="100" Margin="4,0" Foreground="{Binding Progress5hHex}" Background="{Binding ProgressBgHex}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="{Binding CurrentUsage.GeminiResetTime}" Foreground="{Binding TimerTextHex}" FontSize="9" FontWeight="Medium" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>

            <Rectangle Grid.Column="4" Fill="{Binding DividerHex}" Width="1" Margin="0,4"/>

            <!-- Claude & GPT Status -->
            <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,2,0">
                <TextBlock Text="Claude " Foreground="{Binding SubTextHex}" FontSize="9.5"/>
                <TextBlock Text="100%" Foreground="#00E676" FontSize="9.5" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
