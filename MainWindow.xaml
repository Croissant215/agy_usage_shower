<Window x:Class="AgyUsageShower.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AGY Usage Overlay"
        Width="140" Height="32"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Border Background="#181825" CornerRadius="6" BorderBrush="#313244" BorderThickness="1" Margin="2" Cursor="Hand">
        <Border.ToolTip>
            <ToolTip Content="Click for detailed Antigravity usage info"/>
        </Border.ToolTip>

        <Grid Margin="6,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Status Dot -->
            <Ellipse Grid.Column="0" Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#40C057"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentUsage.IsOffline}" Value="True">
                                <Setter Property="Fill" Value="#FA5252"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <!-- Model / Quota Bar -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <DockPanel>
                    <TextBlock Text="AGY" Foreground="#89B4FA" FontSize="10" FontWeight="Bold"/>
                    <TextBlock Text="{Binding QuotaFormatted}" Foreground="#CDD6F4" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                </DockPanel>
                <ProgressBar Height="3" Value="{Binding CurrentUsage.QuotaPercent}" Minimum="0" Maximum="100" Margin="0,2,0,0" Foreground="{Binding QuotaColorHex}" Background="#313244"/>
            </StackPanel>

            <!-- Reset Timer -->
            <TextBlock Grid.Column="2" Text="{Binding CurrentUsage.ResetCountdown}" Foreground="#F9E2AF" FontSize="9" VerticalAlignment="Center" Margin="6,0,0,0"/>
        </Grid>
    </Border>
</Window>
